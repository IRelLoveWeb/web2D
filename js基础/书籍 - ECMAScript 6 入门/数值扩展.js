/*
1.二进制和八进制数值的新的写法，分别用前缀0b（或0B）和0o（或0O）表示
2.Number.isFinite(num) ==>是否有限
  Number.isNaN() ==> 是否非数字
  Number.parseInt === parseInt
  Number.parseFloat === parseFloat
  Number.isInteger(num) ==>是否为整数(只针对数字)
  Number.EPSILON ==>极小的误差

3.Math.trunc(num) ==> 用于去除一个数的小数部分，返回整数部分;
                      非数值用Number转化为数值;空值和无法截取整数的值，返回NaN;

4. 指数运算 Math.pow(num1,num2) ==> num1 ** num2  num2个num1相乘
*/